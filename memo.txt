memo

# やることメモ

+ sendRequestのエラーハンドリング
  >> 一度requestを送った相手に対して再びrequestがおくれないようにする

+ onSnapshot周りの実装
  >> リロード時に前のonSnapshotのリッスンが消えていない説


# request機能実装に必要な機能

+ requestの送信者と受信者のデータの用意　OK!
+ requestを送信するfirebase handler
+ requestの記録を検知して、知らせてくれる機能
    - react側とfirebase側の双方で実装が必要？

# friend chat 機能実装に必要な機能

+ friendsの記録・読み出し(各friendsのuid, 対応するchatroomのid)
+ friendsの記録を書き出すfirebase handler
+ friendsの記録を読みだすfirebase handler
+ chatの記録を行うデータベースの準備(chatroom)
+ chatの記録を検知して、知らせてくれる機能
    - react側とfirebase側の双方で実装が必要？