memo

# やることメモ

+ userDocsArrayから該当ユーザーを取り出す処理
  >> いちいちuidが等しい奴をfor文ぶん回して取得してるので、たとえは<li>のid={someString}-{index of array}みたいな形にして、
     {index of array}部分のみ切り出してNumber化し、配列から直接取り出すとかの工夫をしないとたぶん速度的にネックになる

+ onSnapshot周りの実装
  >> アプリ内に視覚的に通知を表示する機能

+ friendList周りの実装
(1) allUserDocsから該当のfriendを抽出する関数
  >> 1. friend該当者がアカウントを削除した場合のハンドリング
  >> 2. appUserDoc, allUserDocsが変更されるたびにfriendlistを更新してしまうので、差分を検出して変更を反映する方式に最適化できないか

(2) friendsの記録・読み出し(対応するchatroomのid)

(3) chatの記録を行うデータベースの準備(chatroom)

(4) chat機能の実装（既に検討はついてる）

(5) chatの記録を検知して、知らせてくれる機能
    - react側とfirebase側の双方で実装が必要？